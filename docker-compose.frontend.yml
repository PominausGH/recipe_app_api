services:
  frontend:
    build:
      context: ./frontend
    restart: always
    ports:
      - "5173:80"
    networks:
      - recipe_app_api_default

networks:
  recipe_app_api_default:
    external: true
